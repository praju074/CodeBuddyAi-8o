export const showMessage = (text: string, type: "success" | "error" | "info" = "info") => {
  // This function is a placeholder. The actual implementation is in app/page.tsx.
  // This is to avoid circular dependencies and keep the component simple.
  console.log(`Show message: ${text} (${type})`)
}
